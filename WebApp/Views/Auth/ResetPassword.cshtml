@using DataAccess.Vms.Identity
@using Microsoft.AspNetCore.Mvc.Localization
@model ResetPasswordViewModel
@inject IHtmlLocalizer<SharedResource> Localizer
@{
    Layout = "_AuthLayout";
    ViewData["Title"] = Localizer["ResetPassword"];
}

<form asp-action="ResetPassword" method="post" class="space-y-5">
    <input type="hidden" asp-for="Token" />
    <input type="hidden" asp-for="Email" />

    <div>
        <label class="block mb-1 font-medium">@Localizer["NewPassword"]</label>
        <input asp-for="Password" type="password"
               class="w-full px-4 py-2 rounded-lg border border-gray-300 dark:border-gray-700
                      bg-gray-50 dark:bg-gray-800 focus:ring-2 focus:ring-blue-500 outline-none" />
        <span asp-validation-for="Password" class="text-red-500 text-sm"></span>
    </div>

    <div>
        <label class="block mb-1 font-medium">@Localizer["ConfirmPassword"]</label>
        <input asp-for="ConfirmPassword" type="password"
               class="w-full px-4 py-2 rounded-lg border border-gray-300 dark:border-gray-700
                      bg-gray-50 dark:bg-gray-800 focus:ring-2 focus:ring-blue-500 outline-none" />
        <span asp-validation-for="ConfirmPassword" class="text-red-500 text-sm"></span>
    </div>

    <button type="submit"
            class="w-full py-2 rounded-lg bg-green-600 hover:bg-green-700
                   text-white font-semibold transition">
        @Localizer["ResetPassword"]
    </button>
</form>
